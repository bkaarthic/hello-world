pipeline {
    agent any
    stages {
            stage('pub') {
                steps {
                    sshagent(['docker-ssh']) {
                        sh 'ssh -o StrictHostKeyChecking=no -l dockeradmin 3.111.198.19 pwd'
                        }
        }
    }
  }
}
