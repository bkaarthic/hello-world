pipeline {
    agent any
    tools {
        maven 'maven-3'
    }
    stages {
        stage('build') {
            steps {
                sh "mvn clean install -D.maven.test.skip=true"
            }
        }
        stage('unit ') {
            steps {
                sh "mvn surefire-report:report"
            }
        }
  }
}
